#ifndef FLASH_DOWNLOAD_H#define FLASH_DOWNLOAD_H#include <stdint.h>#include <stdbool.h>// ============ 升级状态定义 ============typedef enum{    FW_UPDATE_IDLE = 0,         // 空闲，无升级任务    FW_UPDATE_PREPARING,        // 准备中（擦除Flash）    FW_UPDATE_READY,            // 已就绪，等待数据    FW_UPDATE_TRANSFERRING,     // 数据传输中    FW_UPDATE_VERIFYING,        // 校验中    FW_UPDATE_COMPLETE,         // 升级完成    FW_UPDATE_ERROR             // 升级失败} FlashDownloadState_t;// ============ 升级结果定义 ============typedef enum{    FW_RESULT_OK = 0,           // 成功    FW_RESULT_ADDR_INVALID,     // 地址无效    FW_RESULT_SIZE_TOO_LARGE,   // 数据过大    FW_RESULT_ERASE_FAILED,     // 擦除失败    FW_RESULT_WRITE_FAILED,     // 写入失败    FW_RESULT_VERIFY_FAILED,    // 校验失败    FW_RESULT_SEQUENCE_ERROR,   // 序列号错误    FW_RESULT_BUSY,             // 忙    FW_RESULT_NOT_READY         // 未就绪} FlashDownloadResult_t;// ============ 升级进度信息 ============typedef struct{    uint32_t total_size;        // 总大小（字节）    uint32_t received_size;     // 已接收大小（字节）    uint32_t target_address;    // 目标地址    uint8_t progress_percent;   // 进度百分比} FlashDownloadProgress_t;// ============ 升级配置 ============typedef struct{    uint32_t max_firmware_size;     // 最大固件大小（字节）    uint32_t flash_sector_size;     // Flash扇区大小（字节）    uint32_t user_start_addr;       // 用户可用起始地址    uint32_t user_end_addr;         // 用户可用结束地址    uint8_t verify_enabled;         // 是否启用校验    uint8_t auto_reset_on_complete; // 完成后自动复位} FlashDownloadConfig_t;// ============ DID/RID 定义已移至 uds_did_rid.h ============// 请包含 "uds_did_rid.h" 以使用 DID_FIRMWARE_VERSION 等宏// ============ Flash地址配置 ============#define FW_APP_START_ADDR           0x00034000  /* 应用程序起始地址 */#define FW_APP_MAX_SIZE             0x00020000  /* 最大128KB */#define FW_BOOTLOADER_START_ADDR    0x00000000  /* Bootloader起始地址 */// ============ TBOX 地址映射配置 ============/* TBOX 发送的地址（客户协议定义）*/#define TBOX_ADDR_START             0x08004000  /* TBOX 请求的起始地址 */#define TBOX_ADDR_END               0x08054000  /* TBOX 请求的结束地址（128KB）*//* 地址映射：将 TBOX 地址转换为实际 Flash 地址 */#define MAP_TBOX_ADDR_TO_FLASH(addr) \    (((addr) >= TBOX_ADDR_START && (addr) < TBOX_ADDR_END) ? \     ((addr) - TBOX_ADDR_START + FW_APP_START_ADDR) : (addr))// ============ RAM缓冲区配置 ============#define FW_RAM_BUFFER_SIZE          (60 * 1024)  /* 60KB RAM缓冲区，用于暂存固件数据 *//* Flash写入开关: 0=仅日志不写Flash, 1=实际写入Flash */#define FW_FLASH_WRITE_ENABLED      1// ============ 回调函数类型（供UDS层调用） ============/** * @brief 请求下载回调（对应 UDS 0x34） * @param address 目标地址 * @param size 固件总大小 * @return 处理结果 */FlashDownloadResult_t FlashDownload_OnRequestDownload(uint32_t address, uint32_t size);/** * @brief 传输数据回调（对应 UDS 0x36） * @param block_sequence_number 块序列号（1-255） * @param data 数据指针 * @param len 数据长度（字节） * @return 处理结果 */FlashDownloadResult_t FlashDownload_OnTransferData(uint8_t block_sequence_number,                                                    uint8_t* data,                                                    uint16_t len);/** * @brief 请求传输退出回调（对应 UDS 0x37） * @return 处理结果 */FlashDownloadResult_t FlashDownload_OnTransferExit(void);/** * @brief 擦除固件区（对应 UDS 0x31 RID_ERASE_FIRMWARE） * @param address 起始地址 * @param size 擦除大小 * @return 处理结果 */FlashDownloadResult_t FlashDownload_Erase(uint32_t address, uint32_t size);/** * @brief 计算固件CRC（对应 UDS 0x31 RID_CALCULATE_CRC） * @param address 起始地址 * @param size 计算大小 * @param crc_result 输出CRC结果 * @return 处理结果 */FlashDownloadResult_t FlashDownload_CalculateCRC(uint32_t address, uint32_t size, uint32_t* crc_result);// ============ 状态查询接口 ============/** * @brief 获取当前升级状态 */FlashDownloadState_t FlashDownload_GetState(void);/** * @brief 获取升级进度 */void FlashDownload_GetProgress(FlashDownloadProgress_t* progress);/** * @brief 获取最后一次错误 */FlashDownloadResult_t FlashDownload_GetLastError(void);/** * @brief 获取固件版本号 */uint16_t FlashDownload_GetFirmwareVersion(void);/** * @brief 获取Bootloader版本号 */uint16_t FlashDownload_GetBootloaderVersion(void);/** * @brief 获取当前固件CRC */uint32_t FlashDownload_GetFirmwareCRC(void);// ============ 控制接口 ============/** * @brief 初始化固件升级模块 * @param config 配置参数（可为NULL，使用默认值） */void FlashDownload_Init(const FlashDownloadConfig_t* config);/** * @brief 取消当前升级任务 */void FlashDownload_Cancel(void);/** * @brief 重置升级模块（清空所有状态） */void FlashDownload_Reset(void);// ============ 主循环调用 ============/** * @brief 升级模块主循环处理（处理耗时操作） * @note 需要在主循环中周期性调用 */void FlashDownload_Task(void);/** * @brief 获取是否需要响应等待（NRC 0x78） * @return true=需要返回响应等待 */bool FlashDownload_IsPending(void);#endif /* FLASH_DOWNLOAD_H */